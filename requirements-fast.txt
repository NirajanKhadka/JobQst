# JobQst - Fast Mode (Minimal Dependencies)
# 95% accuracy, 2s startup, works on any machine

# Core web scraping
requests>=2.28.0
beautifulsoup4>=4.11.0
playwright>=1.42.0
aiohttp>=3.9.0

# Database - SQLite focused
sqlalchemy>=2.0.0

# Dashboard - Streamlit only
streamlit>=1.28.0
pandas>=2.0.0
plotly>=5.15.0

# CLI and utilities
rich>=13.3.5
click>=8.1.0
python-dotenv>=1.0.0

# Basic text processing (no ML)
python-docx>=0.8.11

# JobSpy integration
python-jobspy>=1.1.0

# Testing
pytest>=7.3.1

# Total install size: ~300MB (vs 3GB with AI)
# Startup time: ~2s (vs 10s with AI)
# Memory usage: ~200MB (vs 2GB with AI)
