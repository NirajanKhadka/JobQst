# JobQst - AI Mode (Full Features)
# 99% accuracy, 10s startup, requires decent hardware

# Include all fast mode dependencies
-r requirements-fast.txt

# AI/ML stack (optional, heavy)
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=3.0.0
scikit-learn>=1.3.0

# Advanced NLP
spacy>=3.7.0

# GPU acceleration (optional)
# torch[cuda] - uncomment if you have NVIDIA GPU

# Note: Only install this if you need advanced AI features
# Most users should use requirements-fast.txt instead
